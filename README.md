remember to create the a .env file and put your discord bot token in:

DISCORD_TOKEN=your_token_here

# Jimbot

## Setup

```bash
git clone <repo>
cd jimbot

python -m venv .venv
source .venv/bin/activate   # Mac/Linux
.venv\Scripts\activate      # Windows

pip install -r requirements.txt
